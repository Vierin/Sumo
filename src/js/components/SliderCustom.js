import { gsap } from "gsap";

export class SliderCustom {
    constructor() {
        this.view = document.querySelector('[data-component="SliderCustom"]');

        if(this.view) {
            this.init();
        }
    }


    init() {

    }
}